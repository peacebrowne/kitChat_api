import Err from "../error/error.js";
import { DEFAULT_HEADER } from "../util/util.js";

const valid = {

    email: async email => {

        console.log(email)
        // regular expression to validate email
        
        
        // return true if email is valid else return false
        
    },
    contact: async contact => {
        console.log(contact)
        // regular expression to validate contact


        // return true if contact is valid else return false

    }

}

export default valid